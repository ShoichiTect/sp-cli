# TODO

- Add macOS Keychain storage and retrieval for Spotify client credentials so `sp-cli auth` does not require shell env vars or repeated manual entry.
- Add fav slug to sp-cli for managing favorite tracks/albums/artists
